{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "inference-control-plane.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "inference-control-plane.labels" . | nindent 4 }}
{{- end }}
