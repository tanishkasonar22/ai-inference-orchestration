# runtime
grpcio>=1.83.0   # committed stubs need grpcio-tools 1.83.0+
protobuf==7.35.1
kubernetes>=29.0
pyyaml>=6.0

# dev only — regenerating stubs from .proto. Needs a C++ toolchain on Windows,
# or a matching prebuilt wheel. Not needed to run the server; stubs are committed.
# grpcio-tools
